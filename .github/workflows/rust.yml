name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        toolchain:
          - stable
          - beta
          - nightly
    defaults:
      run:
        working-directory: crates

    steps:
    - uses: actions/checkout@v4
    - run: rustup update ${{ matrix.toolchain }} && rustup default ${{ matrix.toolchain }}
    - name: Build type system
      run: cargo build -p relayrl_types --release --verbose
    - name: Build framework (offline)
      run: cargo build -p relayrl_framework --release --no-default-features --features client --verbose
    - name: Build framework (online::zmq)
      run: cargo build -p relayrl_framework --release --no-default-features --features "client zmq-transport" --verbose
    - name: Build framework (online::nats)
      run: cargo build -p relayrl_framework --release --no-default-features --features "client nats-transport" --verbose
    - name: Build framework (online::zmq&nats)
      run: cargo build -p relayrl_framework --release --no-default-features --features "client zmq-transport nats-transport" --verbose
    # - name: Run tests
    #   run: cargo test --release --verbose
    # - name: Run benchmarks
    #   run: cargo bench --verbose
